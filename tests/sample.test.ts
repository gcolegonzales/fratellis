import { describe, it, expect } from 'vitest';

// Toolchain sanity — replaced/augmented by real lib tests in later tasks.
describe('toolchain', () => {
  it('runs vitest', () => {
    expect(1 + 1).toBe(2);
  });
});
